let startValue;
let finalValue;
// Do not change code above!


startValue = startValue  +10 ;
finalValue = startValue++;



console.log(startValue)
//
// ## Inkrementacja
//
// W pliku macie zdefiniowane zmienne `startValue` i `finalValue`.
//
//     Przypiszcie zmiennej `startValue` wartość 10, a następnie z wykorzystaniem inkrementacji
//
// (użycie operatora postinkrementacji) zwiększcie wartość `startValue` o jeden i przypiszcie ją do zmiennej `finalValue`.
//
//     Wyświetlcie wartość `finalValue` w konsoli.
