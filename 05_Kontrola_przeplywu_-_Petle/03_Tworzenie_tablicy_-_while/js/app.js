const numbers = [];
const n = 12;

/**
 * Write your code below!
 */

let i = 1;
while (i <= n) {
    numbers.push(i);
    i++;
}{
    console.log(numbers);
}

// N### Wykorzystajcie do tego zadania pętlę while
//
// Napiszcie program, który na podstawie wartości zmiennej `n` utworzy tablicę
// jednowymiarową w zmiennej `numbers` zawierającą kolejne liczby całkowite od 1 do `n` (włącznie).
//  */
//
